# Kai
A C(++) version of Kai

## Building
```
make
```

### Configuration

`-DDEBUG`:

`-DRELEASE`:

`-DASSERTS`:

Compile Kai with asserts enabled (this is the default for all non-release builds).

`-DNO_BOUNDS_CHECK`:

`-SLOW`:

`-FAST`:

`-DDIAGNOSTICS`:
