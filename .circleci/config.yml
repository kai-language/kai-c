version: 2
jobs:
  build:
    docker:
      - image: kailanguage/linux-ci:0.0.1
    steps:
      - checkout
      - run: ln -s `which llvm-config-6.0` /bin/llvm-config
      - run: make
      - run: make tests

workflows:
  version: 2
  build-and-test:
    jobs:
      - build
